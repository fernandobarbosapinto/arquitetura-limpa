import { GetAllElephantUsecase } from './get-all-elephant.usecase';

describe('GetAllElephant.Usecase', () => {
  it('should create an instance', () => {
    expect(new GetAllElephantUsecase()).toBeTruthy();
  });
});
