import { GetElephantByIdUsecase } from './get-elephant-by-id.usecase';

describe('GetElephantById.Usecase', () => {
  it('should create an instance', () => {
    expect(new GetElephantByIdUsecase()).toBeTruthy();
  });
});
