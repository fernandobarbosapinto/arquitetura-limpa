import { ElephantRepository } from './elephant.repository';

describe('Elephant.Repository', () => {
  it('should create an instance', () => {
    expect(new ElephantRepository()).toBeTruthy();
  });
});
