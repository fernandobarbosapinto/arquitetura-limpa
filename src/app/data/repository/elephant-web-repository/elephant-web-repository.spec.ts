import { ElephantWebRepository } from './elephant-web-repository';

describe('ElephantWebRepository', () => {
  it('should create an instance', () => {
    expect(new ElephantWebRepository()).toBeTruthy();
  });
});
