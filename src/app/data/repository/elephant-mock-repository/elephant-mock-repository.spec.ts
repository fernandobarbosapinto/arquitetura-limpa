import { ElephantMockRepository } from './elephant-mock-repository';

describe('ElephantMockRepository', () => {
  it('should create an instance', () => {
    expect(new ElephantMockRepository()).toBeTruthy();
  });
});
