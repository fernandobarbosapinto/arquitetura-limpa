import { ElephantMockRepositoryMapper } from './elephant-mock-repository-mapper';

describe('ElephantMockRepositoryMapper', () => {
  it('should create an instance', () => {
    expect(new ElephantMockRepositoryMapper()).toBeTruthy();
  });
});
